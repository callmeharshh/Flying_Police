[pytest]
markers =
    unit: fast unit tests (no models, no external APIs)
    validation: regression tests against captured fixtures on disk
    integration: tests that download models or call external APIs

testpaths =
    tests/unit
    tests/validation
    tests/integration
